# Empty constraints for the M15 pad-ring floorplan prototype (Flavor A).
# chip_top has no core logic and no timing paths -- pads only. init_design
# requires a constraint mode, so this file exists purely to satisfy MMMC.
