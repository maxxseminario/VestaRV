%% maestro2tex -- generated table; do not edit by hand.
\begin{table}[htbp]
  \centering
  \caption[{Loop stability measured at the reference electrode with the cell in the loop, by \texttt{stb} analysis\,\dots}]{Loop stability measured at the reference electrode with the cell in the loop, by \texttt{stb} analysis through the feedback probe. The loop gain available at the electrode is an order of magnitude smaller than the amplifier's own gain because the cell loads the high-impedance output node; it is this figure, not the open-circuit gain, that sets the regulation error of Table~\ref{tab:ctrlamp-cell-corners}. The phase margin is correspondingly large --- the loop crosses unity well below any internal pole --- so the control loop is unconditionally stable into this cell but slow, with a unity-gain frequency of a few tens of hertz.}
  \label{tab:ctrlamp-cell-loop}
  \begin{tabular}{lrrrrrrr}
    \toprule
    Parameter & Unit & tt & ff & fs & sf & ss & Spread \\
    \midrule
    DC loop gain $T_0$ & \si{\decibel} & 7.88 & 8.17 & 7.65 & 8.10 & 7.69 & 0.52 \\
    Loop unity-gain frequency & \si{\hertz} & 37.2 & 38.7 & 35.9 & 38.4 & 36.1 & 2.8 \\
    Phase margin & \si{\degree} & 125.2 & 124.9 & 125.5 & 124.9 & 125.5 & 0.7 \\
    \bottomrule
  \end{tabular}
\end{table}
