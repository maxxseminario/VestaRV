%% maestro2tex -- generated Monte Carlo histogram; do not edit by hand.
\providecommand{\MaestroRoot}{include/analog/}
\begin{figure}[htbp]
  \centering
  {\pgfplotsset{compat=1.18}%
  \begin{tikzpicture}
    \begin{axis}[
      width=0.80\linewidth, height=5.6cm,
      xlabel={Bias generator supply current $I_{\mathrm{DD}}$~[\si{\micro\ampere}]}, ylabel={Samples},
      grid=both,
      major grid style={line width=0.2pt, draw=black!14},
      minor grid style={line width=0.2pt, draw=black!7},
      minor tick num=1,
      axis line style={line width=0.4pt, draw=black!45},
      tick style={line width=0.4pt, draw=black!45},
      tick align=outside,
      label style={font=\small}, tick label style={font=\small},
      enlarge x limits=0.02, enlarge y limits=0.10,
      every axis plot/.append style={line join=round, no marks},
      legend style={font=\footnotesize, draw=none, fill=none,
                    at={(0.5,1.02)}, anchor=south, legend columns=-1,
                    /tikz/every even column/.append style={column sep=9pt}},
      legend cell align=left,
      scaled y ticks=false, scaled x ticks=false,
      y tick label style={/pgf/number format/fixed,
                          /pgf/number format/precision=0},
      ymin=0, ymax=28.6,
      enlarge x limits=0.04, enlarge y limits=false,
      area legend
    ]
      \addplot[ybar interval, draw=mtxA, fill=mtxA!25, line width=0.7pt] table {\MaestroRoot data/ctrlamp_mc_bias_00.dat};
      \addlegendentry{Supply current $I_{\mathrm{DD}}$}
      \draw[black!70, line width=1.0pt, densely dashed] ({axis cs:23.467,0}|-{rel axis cs:0,0}) -- ({axis cs:23.467,0}|-{rel axis cs:0,0.90})
        node[above, font=\footnotesize, inner sep=1pt] {tt corner};
    \end{axis}
  \end{tikzpicture}}
  \caption[{Supply current of the local bias generator over 200 process-and-mismatch samples, with the \texttt{tt} corner\,\dots}]{Supply current of the local bias generator over 200 process-and-mismatch samples, with the \texttt{tt} corner at the same trim code marked. Monte Carlo gives \(\sigma/\mu = \SI{12.5}{\percent}\), \(17.7\)--\SI{33.1}{\micro\ampere}, where the five process corners span \SI{1.2}{\percent}, \(23.25\)--\SI{23.52}{\micro\ampere} (Table~\ref{tab:biasgen-dcop}, interpolated to \texttt{BiasAdj}~=~37); the corner sweep is at \SI{27}{\celsius} against \SI{37}{\celsius} here, which accounts for the \SI{0.45}{\micro\ampere} of mean offset and not for the width. A self-biased loop fixes its current by a device-ratio and resistor balance that a global corner shift leaves intact but local mismatch does not, so corner analysis under-reports this term tenfold. It is the largest single contributor to A1's own spread (peak cell current \SI{14.5}{\percent}, \(f_{\mathrm{u}}\) \SI{12.4}{\percent}), leaving roughly \SI{7}{\percent} in quadrature for the amplifier's own devices. This is a separate netlist and a separate draw, so sample \(n\) here is not sample \(n\) in the A1 tests and the two must not be paired.}
  \label{fig:ctrlamp-mc-bias}
\end{figure}
