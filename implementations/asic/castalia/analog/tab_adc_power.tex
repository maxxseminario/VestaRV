%% HAND-WRITTEN table fragment (matches the maestro2tex house style).
%%
%% REPLACED 2026-08-25.  The previous table (I_AVDD 11.84/13.85/15.37 uA,
%% I_DVDD25 2.225/2.304/2.385 uA, I_DVDD10 3.249/3.448/3.698 uA, total power
%% 38.53/43.84/48.07 uW, energy 44.31/50.42/55.27 pJ, 23 clocks / 1.150 us /
%% 869.6 kSa/s) is superseded in full.  Source for the replacement:
%% ~/chips/castalia/simruns/adcchar_20260825/README.md S3 and S5.
%% Why it was replaced rather than corrected:
%%   - the conversion period was wrong by 4x on the stimulus it was measured
%%     with (README S3: READY every 4.600 us), so every energy entry was 4x low;
%%   - the supply currents are reproduced by NO window of that stimulus.  The
%%     ADE XL outputs that produced them evaluate to `error' at every sweep
%%     point -- a wrapping parenthesis pair around average(clip(abs(IT(...))))
%%     (README S6.1) -- and the published row came from a psf post-processor
%%     averaging a window that was not a conversion;
%%   - I_DVDD25 over a whole conversion is 2.0 nA (leakage only), against the
%%     2.30 uA published.
%% Replacement numbers: as-delivered protocol from corners/c_tt_rtl and the
%% maestro_char headless run (maestro/summary_adc_dc.txt, 9 points, all within
%% 0.02 uA of each other); design-rate protocol from corners/corner_table.txt
%% tt row; corner range from the same corner table (cf. tab_adc_corners).
%% Step dependence: the same nominal point reads 6.36 uA on a 63-code sweep
%% step and 12.56 uA on a 220-code step.
\begin{table}[htbp]
  \centering
  \caption[{Conversion timing, supply current and conversion energy on the delivered and design-rate clock protocols.}]{Conversion timing, supply current and conversion energy of the extracted converter on the two clock protocols of Section~\ref{ss:adc}, nominal corner, \SI{37}{\celsius}. Currents are averaged over one whole conversion rather than sampled, and on a 63-code sweep step: the switching term dominates, so the same point reads \SI{12.56}{\micro\ampere} on a 220-code step, and any quoted current must name its step. The corner range is the design-rate protocol across the five corners of Table~\ref{tab:adc-corners}. Over a whole conversion the digital \SI{2.5}{\volt} rail carries leakage only.}
  \label{tab:adc-power}
  \begin{tabular}{llrrr}
    \toprule
    Parameter & Unit & As delivered & Design rate & Corner range \\
    \midrule
    Trigger clock                & \si{\mega\hertz}   & 20 & 20 & 20 \\
    Falling edges per conversion &                    & 10 & 10 & 10 \\
    Conversion period            & \si{\micro\second} & 4.600 & 1.150 & 1.150 \\
    Conversion rate              & kSa/s              & 217.4 & 869.6 & 869.6 \\
    \midrule
    Analog supply current, \SI{2.5}{\volt}  & \si{\micro\ampere} & 4.737 & 6.360 & 4.79--15.43 \\
    Digital supply current, \SI{2.5}{\volt} & \si{\nano\ampere}  & 2.0 & --- & --- \\
    Digital supply current, \SI{1.0}{\volt} & \si{\micro\ampere} & 1.09 & --- & --- \\
    Total power                             & \si{\micro\watt}   & 12.95 & 18.09 & 15.77--43.06 \\
    Energy per conversion                   & \si{\pico\joule}   & 59.6 & 20.8 & 18.1--49.5 \\
    \midrule
    Fitted slope                            & codes/V    & 628.85 & 628.59 & 628.13--628.73 \\
    Input at code 0                         & \si{\volt} & 0.4410 & 0.4408 & 0.4399--0.4408 \\
    \bottomrule
  \end{tabular}
\end{table}
