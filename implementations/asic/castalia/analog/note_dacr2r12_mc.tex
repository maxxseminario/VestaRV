% HAND-WRITTEN fragment -- not generated by maestro2tex, but placed by it via the
% `order` list in configs/tb_anatop_pixel_dacR2R12_corners.json.
%
% Numbers here come from tab_dacr2r12_mc_ideal, tab_dacr2r12_mc_scale and
% tab_dacr2r12_mc_wpsu, and from the corner table tab_dacr2r12_wpsu. Re-read them
% from those tables if either run is repeated.
%
% Source: Plan.2.Run.3, 200 samples, seed 12345, LDS sampling, variations=all,
% section castalia_pm_25, 25 C on both DAC benches. Verified before use: all 64
% ladder RS*/RP*/RTERM* instances netlist mismatchflag=1, and every test selects
% castalia_pm_25. Two earlier Monte Carlo attempts on this block failed one or
% other of those checks and produced tidy distributions carrying no matching
% information; their numbers must not be reintroduced.

\subsubsection{Matching}\label{sss:dacr2r12-mc}

R-2R linearity is a matching property, so it is the Monte Carlo run and not the corner
set that measures it. On an ideal \SI{2.5}{\volt} rail, over 200 samples varying process
and mismatch together, INL is \SI{1.49}{LSB} mean against a \SI{1}{LSB} limit and the
worst sample reaches \SI{3.45}{LSB}; DNL reaches \SI{-6.61}{LSB}
(Table~\ref{tab:dacr2r12-mc-ideal}). \SI{21.0}{\percent} of samples meet
\(\mathrm{INL} < \SI{1}{LSB}\) and \SI{40.0}{\percent} are monotonic
(\(\mathrm{DNL} > \SI{-1}{LSB}\)); \SI{17.0}{\percent} meet both, which over four
independent channels is a \SI{0.08}{\percent} chip yield.

The failure is matching and nothing else. Full scale, LSB and gain error are set by the
rail and by ladder attenuation, not by device ratios, and they barely move across the
same 200 samples: \(\sigma\) is \SI{1.8}{\micro\volt} on a \SI{2.499}{\volt} full scale
and \SI{0.5}{ppm} on a \SI{-76.9}{ppm} gain error (Table~\ref{tab:dacr2r12-mc-scale}).
All of the spread in Table~\ref{tab:dacr2r12-mc-ideal} is therefore resistor mismatch in
the ladder.

Referencing the ladder to \texttt{VddDac} instead of an ideal rail does not change this.
The ratiometric figures of Table~\ref{tab:dacr2r12-mc-wpsu} --- \SI{1.51}{LSB} mean INL,
\SI{-1.75}{LSB} mean DNL, the same \SI{40.0}{\percent} monotonic --- reproduce the
ideal-rail figures to within \SI{2}{\percent}, because dividing the reference out removes
the supply block and leaves the same ladder. The raw columns beside them, \SI{36.8}{LSB} and
\SI{-10.8}{LSB}, carry the reference movement of Section~\ref{sss:dacr2r12-supply} on top.
The two defects are additive and independent, and either alone fails the block.

The corner set sees none of it. Ratiometric INL spans \SIrange{0.432}{0.450}{LSB} across
ff, fs, sf and ss --- a \SI{4}{\percent} spread --- while the Monte Carlo samples of the
same quantity scatter from \SI{0.35}{LSB} to \SI{3.49}{LSB}. A process corner moves every
ladder resistor in the same direction, which a ratio cancels; the corner table is context
and not a linearity result.

INL and DNL are both skewed (Fisher skew \num{+0.74} and \num{-1.11}), so the tables
quote the median, the first and ninety-ninth percentiles and the worst sample rather than
mean \(\pm\,3\sigma\).
