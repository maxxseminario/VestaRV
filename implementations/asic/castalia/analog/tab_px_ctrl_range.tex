%% maestro2tex -- generated table; do not edit by hand.
\begin{table}[htbp]
  \centering
  \caption[{RE tracking range and TIA compliance range by process corner, conditions as Table~\ref{tab:px-ctrl-tracking}.}]{RE tracking range and TIA compliance range by process corner, conditions as Table~\ref{tab:px-ctrl-tracking}. Both are the width of the region over which the respective error stays inside a \SI{10}{\milli\volt} threshold; the threshold is on the total error including the loop's static offset, so a corner with more offset reports a narrower range for the same underlying loop gain -- read these next to Table~\ref{tab:px-ctrl-tracking} rather than as an independent compliance figure. The TIA range is $\pm\SI{35}{\micro\ampere}\times\SI{35}{\kilo\ohm}$ at the \SI{10}{\kilo\ohm} electrode used here.}
  \label{tab:px-ctrl-range}
  \begin{tabular}{lrrrrrrr}
    \toprule
    Parameter & Unit & tt & ff & fs & sf & ss & Spread \\
    \midrule
    RE tracking range (10 mV crit.) & \si{\volt} & 2.382 & 2.386 & 2.383 & 2.382 & 2.379 & 0.007 \\
    TIA compliance range (10 mV crit.) & \si{\volt} & 0.805 & 0.806 & 0.805 & 0.805 & 0.804 & 0.002 \\
    \bottomrule
  \end{tabular}
\end{table}
