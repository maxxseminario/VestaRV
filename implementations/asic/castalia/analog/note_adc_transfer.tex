% HAND-WRITTEN fragment -- placed by Adc.tex. Numbers from tab_adc_transfer.tex,
% fig_adc_transfer.tex and fig_adc_error.tex, all derived from
% simarchive/tb_anatop_pixel_adc/adc_transfer.csv.
%
% 2026-08-25: the MECHANISM paragraph was WRONG and is replaced.  Source
% ~/chips/castalia/simruns/adcchar_20260825/README.md S1, with cdac_array.txt
% (the plate-by-plate extraction) and probes/probe_table.txt (7 runs):
%   1.535 = 1.235 (differencing topology, sum of physical plates / sum of
%           weights) x 1.243 (top-plate parasitic + comparator input).
%   Extracted DACIO->VSSA25 parasitic is 0.226 x the physical array, NOT the
%   0.535 x C_array this fragment used to infer.
%   Closed form slope = C_top,total/(V_ref*C_unit) -> 627.8 vs 628.59 measured.
%   Probes: +100 fF and +300 fF on DACIO predict 721.9 / 908.5 against measured
%   722.09 / 908.50 (0.03 % / 0.00 %); vdd25 2.5->2.0 V scales the slope by
%   exactly 2.5/2.0 and moves the centre to 1.003 V; forcing the comparator
%   reference to 1.0 V moves the centre to 1.000 V and leaves the slope at
%   628.57 (0.003 %); halving / removing the N-side plates takes the worst
%   residual from 0.50 LSB to 7.02 / 10.36 LSB.
% NUMBERS IN THIS SECTION ARE UNCHANGED -- only the explanation was wrong.
% Absolute capacitances are deliberately not stated (see note_adc_intro).

\subsubsection{Transfer Function}\label{sss:adc-transfer}

The converter is monotonic across the whole sweep, reading code 0 at
\SI{0}{\volt} and code 1023 at \SI{2.5}{\volt} (Figure~\ref{fig:adc-transfer}).
Codes climb 1.535 times faster than ideal: a least-squares fit to the 24
unclipped points of a \SI{0.0625}{\volt} sweep over \SIrange{0.5}{2.0}{\volt} gives \num{628.64} codes per
volt against the ideal \num{409.60}, so the effective full-scale input span is
\SI{1.627}{\volt} rather than the \SI{2.5}{\volt} supply span, and the effective
LSB is \SI{1.591}{\milli\volt} rather than \SI{2.441}{\milli\volt}
(Table~\ref{tab:adc-transfer}). Extrapolating that fit puts code 0 at
\SI{0.441}{\volt} and code 1023 at \SI{2.068}{\volt}, a window centred on
\SI{1.255}{\volt} --- the \SI{1.25}{\volt} channel common mode. The converter
clips outside that window, losing \SI{34.9}{\percent} of the intended input
range, split between the two ends rather than concentrated at one: a
full-scale transimpedance swing about $V_{\mathrm{CM}}$ is truncated
symmetrically.

\paragraph{Mechanism.} The 1.535 factor is a product of two, and the smaller of
them is the parasitic. The array is a \emph{differencing} capacitor DAC: each bit
drives two plates in antiphase, so the charge it injects is proportional to the
difference of the two, and it is that difference which is binary --- to four
figures over the 512:1 weight range --- while the physical plates are not; the
positive and negative segments of each bit row are visible in
Figure~\ref{fig:adc-capdac}. Building the sub-LSB weights as small differences of
much larger plates is what makes them matchable, and the price is that all of
that plate loads the top node:
the summed physical plate is 1.235 times the summed weight. The extracted
top-plate parasitic and the comparator input add a further 1.243, and
\(1.235\times 1.243 = 1.535\). The parasitic alone is \num{0.226} of the physical
array, not the \num{0.535} of the array a single-mechanism reading infers. The
slope then closes in one expression,
\[
  m \;=\; \frac{C_{\mathrm{top,total}}}{V_{\mathrm{ref}}\,C_{\mathrm{unit}}},
\]
giving \num{627.8} codes per volt against the \num{628.59} the corner staircase
measures (Table~\ref{tab:adc-corners}), \SI{0.13}{\percent}.

Seven perturbation runs separate the terms. Two known capacitances added to the
top plate predict the perturbed slope from one fitted \(C_{\mathrm{top}}\) to
better than \SI{0.03}{\percent}, with nothing else in the converter changed.
Lowering the analog supply scales the slope by exactly the reference ratio and
moves the window centre with it, while forcing the comparator's reference alone
moves the centre and leaves the slope unchanged to \SI{0.003}{\percent} --- so
the span is set by the reference and the centre by the comparator's own
supply-halving resistor divider, and the \SI{1.25}{\volt} centre is that divider
rather than an offset. Halving and then removing the antiphase plates moves the
slope the predicted way and destroys the binary weighting at the same time,
taking the worst residual about the fitted line from \num{0.50}~LSB to
\num{7.02} and \num{10.36}~LSB. Nothing here is a layout defect that a
parasitic-free layout would remove: the larger factor is the topology.

Departure from the fitted line is at most \num{0.52}~LSB over the fitted range
(Figure~\ref{fig:adc-error}). That is a residual of a 24-point fit at
\num{39} codes per step, not a linearity figure.

Referred to its own range instead of the nominal one the picture separates
(Table~\ref{tab:adc-transfer}). The usable window is \SIrange{0.441}{2.068}{\volt},
\SI{1.627}{\volt} wide against the \SI{2.5}{\volt} span and centred on the
common mode (Figure~\ref{fig:adc-range}); \SI{34.9}{\percent} of the nominal
range is unreachable, split \SI{0.441}{\volt} low and \SI{0.432}{\volt} high.
Across that window the effective LSB is \SI{1.591}{\milli\volt} rather than
\SI{2.441}{\milli\volt}, and each code sits within \num{0.523}~LSB of the
measured line, \num{0.294}~LSB rms (Figure~\ref{fig:adc-inl}). The two defects
are therefore separable: the range is short by a third, and within what remains
the transfer is straight to better than half a code at the sampled inputs.
