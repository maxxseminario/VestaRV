%% maestro2tex -- generated Monte Carlo histogram; do not edit by hand.
\providecommand{\MaestroRoot}{include/analog/}
\begin{figure}[htbp]
  \centering
  {\pgfplotsset{compat=1.18}%
  \begin{tikzpicture}
    \begin{axis}[
      width=0.86\linewidth, height=6.0cm,
      xlabel={Zero-current offset~[LSB]}, ylabel={Samples},
      grid=both,
      major grid style={line width=0.2pt, draw=black!14},
      minor grid style={line width=0.2pt, draw=black!7},
      minor tick num=1,
      axis line style={line width=0.4pt, draw=black!45},
      tick style={line width=0.4pt, draw=black!45},
      tick align=outside,
      label style={font=\small}, tick label style={font=\small},
      enlarge x limits=0.02, enlarge y limits=0.10,
      every axis plot/.append style={line join=round, no marks},
      legend style={font=\footnotesize, draw=none, fill=none,
                    at={(0.5,1.02)}, anchor=south, legend columns=-1,
                    /tikz/every even column/.append style={column sep=9pt}},
      legend cell align=left,
      scaled y ticks=false, scaled x ticks=false,
      y tick label style={/pgf/number format/fixed,
                          /pgf/number format/precision=0},
      ymin=0, ymax=26,
      enlarge x limits=0.04, enlarge y limits=false,
      area legend
    ]
      \addplot[ybar interval, draw=mtxA, fill=mtxA!25, line width=0.7pt] table {\MaestroRoot data/px_mc_izero_35k_00.dat};
      \addlegendentry{Zero-current offset in LSB, $R_f = \SI{35}{\kilo\ohm}$}
      \draw[black!70, line width=1.0pt, densely dashed] ({axis cs:-1,0}|-{rel axis cs:0,0}) -- ({axis cs:-1,0}|-{rel axis cs:0,0.90})
        node[above, font=\footnotesize, inner sep=1pt] {$-1$};
      \draw[black!70, line width=1.0pt, densely dashed] ({axis cs:1,0}|-{rel axis cs:0,0}) -- ({axis cs:1,0}|-{rel axis cs:0,0.90})
        node[above, font=\footnotesize, inner sep=1pt] {$+1$};
    \end{axis}
  \end{tikzpicture}}
  \caption[{Zero-current output offset at $R_f = \SI{35}{\kilo\ohm}$, in nominal \SI{2.441}{\milli\volt} ADC LSB.}]{Zero-current output offset at $R_f = \SI{35}{\kilo\ohm}$, in nominal \SI{2.441}{\milli\volt} ADC LSB. \textbf{Mismatch only}, \SI{25}{\celsius} nominal process, 200 samples, $R_{\mathrm{ct}} = \SI{10}{\kilo\ohm}$ electrode. The limit shown is the $\pm 1$~LSB entered on the test in Assembler -- comparable in kind across the four gain taps but mechanically harder to meet as $R_f$ rises, since the underlying offset is closer to gain-independent in current (Table~\ref{tab:px-mc-izero}). The converter's measured step is \SI{1.591}{\milli\volt}, not the nominal \SI{2.441}{\milli\volt} (Table~\ref{tab:adc-transfer}); multiply the axis and the limit by \num{1.535} to read them as converter codes. Statistics are in Table~\ref{tab:px-mc-izero}.}
  \label{fig:px-mc-izero-35k}
\end{figure}
