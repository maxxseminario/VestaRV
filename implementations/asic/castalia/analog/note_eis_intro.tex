% HAND-WRITTEN fragment -- NOT generated by maestro2tex. Input from
% AnalogChapter.tex, first of the impedance section.
%
% ======================================================================
% CUT 2026-08-26, OWNER'S EXPLICIT INSTRUCTION: "a few paragraphs and a few
% figures, not a subsection tree".  The impedance section ran ~10 pages over
% seven prose fragments and five figures; it is now three fragments
% (note_eis_intro, note_eis_accuracy, note_eis_rules), three figures and no
% subsubsections.  \label{ss:eis} is kept; every sss:eis-* label is gone.
%
% WHAT WAS CUT FROM THIS FRAGMENT, AND WHERE IT IS:
%   - the run inventory (30 ac runs, 24 fixed-ratio runs, 17 real-DAC
%     transients, 252 corner runs, eight Monte Carlo runs, the extracted-
%     converter record):  ~/chips/castalia/simruns/eisdemo_20260825/README.md,
%     eiscorner_20260825/README.md and eisdeep_20260826/README.md.
%   - the per-corner and per-load numbers behind the caveat paragraph:
%     eiscorner_20260825 corner_tables.txt, ac_resmim/resmim_table.txt;
%     eisdeep_20260826 S5 (band_dense/, band_ff40/, band_ss40/).
%   - the placement argument for the section, and the four lettered firmware
%     architectures: superseded by the cut; the architectures are named in
%     prose in note_eis_accuracy's header.
%   - the whole DROPPED fragments note_eis_coverage.tex, note_eis_excitation.tex
%     and note_eis_open.tex, and the figures fig_eis_error.tex and
%     fig_eis_settling.tex, stay ON DISK, uninput, with their own headers and
%     source lists intact.  Restoring the section is putting their \input lines
%     back in AnalogChapter.tex.
%   - narrative summary of the whole campaign:
%     ~/vestarv/docs/afe_rev2_eis_lite.html.
% NOTHING MEASURED IS LOST.  Every number cut here is in those runs.
% ======================================================================
%
% SOURCE OF EVERY SURVIVING NUMBER IN THIS FRAGMENT:
%   ~/chips/castalia/simruns/eisdemo_20260825/README.md -- S3 the measurement,
%     S5 coverage; engine skill/eisdemo_engine.py, extractor
%     skill/eisdemo_extract.py, all runs textual splices of the pixel's own
%     Maestro-netlisted deck simruns/mcflags_20260825/canon/input.scs.
%   ~/chips/castalia/simruns/eiscorner_20260825/README.md -- S2.5
%     (ac_resmim/resmim_table.txt) is the -33.4 % / +35.5 % on R_f.
%   ~/chips/castalia/simruns/eisdeep_20260826/README.md -- S3 the grid-density
%     argument (10 points/decade puts the chord polygon 0.651 % of the arc
%     radius off the true arc against a 0.30 % instrument error; 20 gives
%     0.165 %, 30 gives 0.074 %), S5 the cornered-poly band edge, S7 the
%     Monte Carlo (sigma = 11.1 % on R_f, n = 654, exact chi2 95 % interval).
%   Monte Carlo throughout: castalia_pm_25, nominal corner, 37 C, seed 12345.
%
% The pinned-passive caveat's generic mechanism lives ONCE in AnalogChapter.tex
% (label p:pinned-passives).  Do not restate it here; this paragraph keeps only
% the pointer and the numbers that are this section's own.
% Library cell names are deliberately kept OUT of the rendered prose (owner
% request 2026-08-21); they live in these comments instead.

\subsection{Impedance Spectroscopy}\label{ss:eis}

The channel measures impedance with no impedance hardware. A small excitation
rides on the reference-DAC setpoint of Section~\ref{ss:dacr2r12}, A1 applies it
at the reference electrode, A2 and the converter read the resulting current
through the same programmable feedback resistor a dc measurement uses
(Section~\ref{ss:tiarprog}), and firmware demodulates the two records in
quadrature --- an I/Q multiply--accumulate over whole excitation periods, no
window, no detrending. The excitation is a toggle between two DAC codes, so the
mode adds a clock divider and an accumulate to the channel and nothing else.

Everything below runs on the netlist of one complete channel --- reference DAC,
both amplifiers, the programmable feedback resistor and the converter --- at a
\SI{2.5}{\volt} analog supply, and is schematic-level except where the converter
appears, which it does as its extracted view. The nominal point is the typical
process corner at \SI{37}{\celsius}; only the small-signal measurement was
repeated over the chapter's four process corners at \SI{25}{\celsius}, over
\SIrange{0}{85}{\celsius}, and over Monte Carlo. The electrodes are the linear
cell model of Section~\ref{ss:analog-models} with
\(R_u = \SI{1}{\kilo\ohm}\) throughout; \emph{fresh}, \emph{typical} and
\emph{fouled} name \(R_{\mathrm{ct}} \parallel C_{\mathrm{dl}}\) of
\SI{1}{\mega\ohm} \(\parallel\) \SI{100}{\nano\farad},
\SI{100}{\kilo\ohm} \(\parallel\) \SI{1}{\micro\farad} and
\SI{10}{\kilo\ohm} \(\parallel\) \SI{10}{\micro\farad}. Sweeps carry \num{20}
points per decade, which puts the chord through the plotted points
\SI{0.165}{\percent} of the arc radius off the true arc against an instrument
error of \SI{0.30}{\percent} (Figure~\ref{fig:eis-nyquist}).

\input{include/analog/fig_eis_nyquist.tex}

\textbf{The pinned-passive caveat of page~\pageref{p:pinned-passives} governs
every corner number in this section.} Neither the ladder that is \(R_f\) nor the
capacitor that sets the feedback pole corners here. Cornering the poly as well
moves \(R_f\) by \(-33.4\,\%\) and \(+35.5\,\%\), and moves the phase band edge
by a factor of three at a frozen noise gain. The Monte Carlo does vary the poly,
and measures the same quantity as \(\sigma = \SI{11.1}{\percent}\).
