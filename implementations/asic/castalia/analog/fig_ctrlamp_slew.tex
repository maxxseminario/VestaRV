%% maestro2tex -- generated figure; do not edit by hand.
\providecommand{\MaestroRoot}{include/analog/}
\begin{figure}[htbp]
  \centering
  {\pgfplotsset{compat=1.18}%
  \begin{tikzpicture}
    \begin{axis}[
      width=0.80\linewidth, height=6.0cm,
      xlabel={Time~[\si{\micro\second}]}, ylabel={Voltage~[\si{\volt}]},
      grid=both,
      major grid style={line width=0.2pt, draw=black!14},
      minor grid style={line width=0.2pt, draw=black!7},
      minor tick num=1,
      axis line style={line width=0.4pt, draw=black!45},
      tick style={line width=0.4pt, draw=black!45},
      tick align=outside,
      label style={font=\small}, tick label style={font=\small},
      enlarge x limits=0.02, enlarge y limits=0.10,
      every axis plot/.append style={line join=round, no marks},
      legend style={font=\footnotesize, draw=none, fill=none,
                    at={(0.5,1.02)}, anchor=south, legend columns=-1,
                    /tikz/every even column/.append style={column sep=9pt}},
      legend cell align=left,
      scaled y ticks=false, scaled x ticks=false,
      y tick label style={/pgf/number format/fixed,
                          /pgf/number format/precision=1}
    ]
      \addplot[mtxA, line width=1.1pt, solid] table {\MaestroRoot data/ctrlamp_slew_00.dat};
      \addlegendentry{$V_{\mathrm{INP}}$}
      \addplot[mtxB, line width=1.1pt, dashed] table {\MaestroRoot data/ctrlamp_slew_01.dat};
      \addlegendentry{$V_{\mathrm{OUT}}$}
    \end{axis}
  \end{tikzpicture}}
  \caption{Large-signal step response. The open-loop bench slams the differential input from \(-\SI{1.25}{\volt}\) to \(+\SI{1.25}{\volt}\) and back, fully steering the input pair, so the output slews at the rate the output stage can charge and discharge the \SI{10}{\pico\farad} load. The two edges are visibly unequal: the output takes \SI{1.4}{\micro\second} to slew up and \SI{5.7}{\micro\second} to slew back down, a factor of four.}
  \label{fig:ctrlamp-slew}
\end{figure}
