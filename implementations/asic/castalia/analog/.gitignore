# Standalone proof-sheet build artifacts (preview_*.tex is tracked, its output is not).
preview*.pdf
preview*.aux
preview*.log
preview*.out

# Scratch page renders from visual checking (pdftoppm -png).
pv-*.png
pvx-*.png
