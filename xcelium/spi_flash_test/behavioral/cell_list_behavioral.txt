# Sources for the standalone SPI-flash (XIP extended-memory) testbench.
# MCU_MP tree (the 0x20000 XIP decode + flash FSM live only here).
# Dependency order — VHDL is order-sensitive.

# --- core constants / memory map ---
../../../hdl/MCU_MP/constants.vhd
../../../hdl/MCU_MP/MemoryMap.vhd

# --- simulation support ---
../../../hdl/MCU_MP/sim/ClkGate.vhd

# --- shared testbench package (scoreboard + register-bus BFM) ---
../../../hdl/MCU_MP/tb/periph_tb_pkg.vhd

# --- flash model + SPI peripheral under test ---
../../../hdl/MCU_MP/tb/serial_flash.vhd
../../../hdl/MCU_MP/periph/SPI.vhd

# --- the self-checking testbench (top-level snapshot) ---
../../../hdl/MCU_MP/tb/SPI_flash_tb.vhd
