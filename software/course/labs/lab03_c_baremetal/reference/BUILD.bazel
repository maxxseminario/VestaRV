# lab03_c_baremetal/reference: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab03_c_baremetal")

# Byte identical to the bench image in rcf/xlab03_c_baremetal.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab03_c_baremetal_flashed_rcf_test",
    image = "xlab03_c_baremetal.rcf",
    golden = "testdata/lab03_c_baremetal_flashed_rcf_golden.txt",
)
