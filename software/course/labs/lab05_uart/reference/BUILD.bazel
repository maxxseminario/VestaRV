# lab05_uart/reference: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab05_uart")

# Byte identical to the bench image in rcf/xxxxxxxxlab05_uart.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab05_uart_flashed_rcf_test",
    image = "xxxxxxxxlab05_uart.rcf",
    golden = "testdata/lab05_uart_flashed_rcf_golden.txt",
)
