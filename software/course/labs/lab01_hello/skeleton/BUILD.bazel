# lab01_hello/skeleton: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab01_hello")

# Byte identical to the bench image in rcf/xxxxxxxlab01_hello.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab01_hello_flashed_rcf_test",
    image = "xxxxxxxlab01_hello.rcf",
    golden = "testdata/lab01_hello_flashed_rcf_golden.txt",
)
