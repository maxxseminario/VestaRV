# lab11_npu/skeleton: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab11_npu")

# Byte identical to the bench image in rcf/xxxxxxxxxlab11_npu.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab11_npu_flashed_rcf_test",
    image = "xxxxxxxxxlab11_npu.rcf",
    golden = "testdata/lab11_npu_flashed_rcf_golden.txt",
)
