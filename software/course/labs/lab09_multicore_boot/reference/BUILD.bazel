# lab09_multicore_boot/reference: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab09_mpboot")

# Byte identical to the bench image in rcf/xxxxxxlab09_mpboot.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab09_mpboot_flashed_rcf_test",
    image = "xxxxxxlab09_mpboot.rcf",
    golden = "testdata/lab09_mpboot_flashed_rcf_golden.txt",
)
