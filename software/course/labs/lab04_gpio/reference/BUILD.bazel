# lab04_gpio/reference: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab04_gpio")

# Byte identical to the bench image in rcf/xxxxxxxxlab04_gpio.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab04_gpio_flashed_rcf_test",
    image = "xxxxxxxxlab04_gpio.rcf",
    golden = "testdata/lab04_gpio_flashed_rcf_golden.txt",
)
