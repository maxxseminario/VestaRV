# lab10_sync/reference: built exactly as software/course/sdk/course.mk builds it.

load("//toolchains/riscv:defs.bzl", "course_lab", "firmware_image_test")

package(default_visibility = ["//visibility:public"])

course_lab(name = "lab10_sync")

# Byte identical to the bench image in rcf/xxxxxxxxlab10_sync.rcf, which the
# instructor simulator flow reads. cmp verified at the time this was written.
firmware_image_test(
    name = "lab10_sync_flashed_rcf_test",
    image = "xxxxxxxxlab10_sync.rcf",
    golden = "testdata/lab10_sync_flashed_rcf_golden.txt",
)
